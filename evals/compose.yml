services:
  evals:
    build:
      context: ..
      dockerfile: evals/Dockerfile
    env_file:
      - ../.env
    environment:
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434/v1}
    volumes:
      - ../data:/app/data
